{
  "name": "sang-event-crm-functions",
  "description": "Cloud Functions for Sang Event CRM",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions:eventcrm,firestore,auth",
    "deploy": "npm run build && firebase deploy --only functions:eventcrm"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "private": true
}
